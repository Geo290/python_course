# set signfica grupo o conjunto

primer = {1,1,2,2,3,4}
segundo = [3,4,5]
segundo = set(segundo) #convertir lista en set

#print(primer | segundo) #el operador '|' (union) une dos sets
#print(primer & segundo) #el operador '&' (interseccion) busca los elementos iguales en cada set
#print(primer - segundo) #el operador '-' (diferencia) busca los elemntos distintos de cada set#
#print(primer ^ segundo) #el operador '^' (diferencia simetrica) elimina los datos que no hay en el otro set 
#print(segundo)